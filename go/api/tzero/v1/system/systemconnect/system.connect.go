// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tzero/v1/system/system.proto

package systemconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	system "github.com/t-0-network/provider-sdk/go/api/tzero/v1/system"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SystemServiceName is the fully-qualified name of the SystemService service.
	SystemServiceName = "tzero.v1.system.SystemService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SystemServiceHealthProcedure is the fully-qualified name of the SystemService's Health RPC.
	SystemServiceHealthProcedure = "/tzero.v1.system.SystemService/Health"
)

// SystemServiceClient is a client for the tzero.v1.system.SystemService service.
type SystemServiceClient interface {
	// *
	// Health returns the list of RPC services registered on this provider,
	// the server's current wall-clock time, and the SDK build identifying
	// this deployment. The returned `services` list always includes
	// "tzero.v1.system.SystemService" itself.
	Health(context.Context, *connect.Request[system.HealthRequest]) (*connect.Response[system.HealthResponse], error)
}

// NewSystemServiceClient constructs a client for the tzero.v1.system.SystemService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSystemServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SystemServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	systemServiceMethods := system.File_tzero_v1_system_system_proto.Services().ByName("SystemService").Methods()
	return &systemServiceClient{
		health: connect.NewClient[system.HealthRequest, system.HealthResponse](
			httpClient,
			baseURL+SystemServiceHealthProcedure,
			connect.WithSchema(systemServiceMethods.ByName("Health")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
	}
}

// systemServiceClient implements SystemServiceClient.
type systemServiceClient struct {
	health *connect.Client[system.HealthRequest, system.HealthResponse]
}

// Health calls tzero.v1.system.SystemService.Health.
func (c *systemServiceClient) Health(ctx context.Context, req *connect.Request[system.HealthRequest]) (*connect.Response[system.HealthResponse], error) {
	return c.health.CallUnary(ctx, req)
}

// SystemServiceHandler is an implementation of the tzero.v1.system.SystemService service.
type SystemServiceHandler interface {
	// *
	// Health returns the list of RPC services registered on this provider,
	// the server's current wall-clock time, and the SDK build identifying
	// this deployment. The returned `services` list always includes
	// "tzero.v1.system.SystemService" itself.
	Health(context.Context, *connect.Request[system.HealthRequest]) (*connect.Response[system.HealthResponse], error)
}

// NewSystemServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSystemServiceHandler(svc SystemServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	systemServiceMethods := system.File_tzero_v1_system_system_proto.Services().ByName("SystemService").Methods()
	systemServiceHealthHandler := connect.NewUnaryHandler(
		SystemServiceHealthProcedure,
		svc.Health,
		connect.WithSchema(systemServiceMethods.ByName("Health")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.system.SystemService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SystemServiceHealthProcedure:
			systemServiceHealthHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSystemServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSystemServiceHandler struct{}

func (UnimplementedSystemServiceHandler) Health(context.Context, *connect.Request[system.HealthRequest]) (*connect.Response[system.HealthResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.system.SystemService.Health is not implemented"))
}
