// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tzero/v1/payment/quote.proto

package paymentconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	payment "github.com/t-0-network/provider-sdk/go/api/tzero/v1/payment"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// QuoteServiceName is the fully-qualified name of the QuoteService service.
	QuoteServiceName = "tzero.v1.payment.QuoteService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// QuoteServiceGetQuotesProcedure is the fully-qualified name of the QuoteService's GetQuotes RPC.
	QuoteServiceGetQuotesProcedure = "/tzero.v1.payment.QuoteService/GetQuotes"
)

// QuoteServiceClient is a client for the tzero.v1.payment.QuoteService service.
type QuoteServiceClient interface {
	// * Lists the active pay-out quotes the caller can transact against, grouped by currency, then
	// payment method: for each, the payout providers it holds a credit line with, the caller's
	// balance with each, and their tiered rate bands. Indicative — request a priced quote via
	// GetQuote to act on one.
	GetQuotes(context.Context, *connect.Request[payment.GetQuotesRequest]) (*connect.Response[payment.GetQuotesResponse], error)
}

// NewQuoteServiceClient constructs a client for the tzero.v1.payment.QuoteService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewQuoteServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) QuoteServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	quoteServiceMethods := payment.File_tzero_v1_payment_quote_proto.Services().ByName("QuoteService").Methods()
	return &quoteServiceClient{
		getQuotes: connect.NewClient[payment.GetQuotesRequest, payment.GetQuotesResponse](
			httpClient,
			baseURL+QuoteServiceGetQuotesProcedure,
			connect.WithSchema(quoteServiceMethods.ByName("GetQuotes")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
	}
}

// quoteServiceClient implements QuoteServiceClient.
type quoteServiceClient struct {
	getQuotes *connect.Client[payment.GetQuotesRequest, payment.GetQuotesResponse]
}

// GetQuotes calls tzero.v1.payment.QuoteService.GetQuotes.
func (c *quoteServiceClient) GetQuotes(ctx context.Context, req *connect.Request[payment.GetQuotesRequest]) (*connect.Response[payment.GetQuotesResponse], error) {
	return c.getQuotes.CallUnary(ctx, req)
}

// QuoteServiceHandler is an implementation of the tzero.v1.payment.QuoteService service.
type QuoteServiceHandler interface {
	// * Lists the active pay-out quotes the caller can transact against, grouped by currency, then
	// payment method: for each, the payout providers it holds a credit line with, the caller's
	// balance with each, and their tiered rate bands. Indicative — request a priced quote via
	// GetQuote to act on one.
	GetQuotes(context.Context, *connect.Request[payment.GetQuotesRequest]) (*connect.Response[payment.GetQuotesResponse], error)
}

// NewQuoteServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewQuoteServiceHandler(svc QuoteServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	quoteServiceMethods := payment.File_tzero_v1_payment_quote_proto.Services().ByName("QuoteService").Methods()
	quoteServiceGetQuotesHandler := connect.NewUnaryHandler(
		QuoteServiceGetQuotesProcedure,
		svc.GetQuotes,
		connect.WithSchema(quoteServiceMethods.ByName("GetQuotes")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.payment.QuoteService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case QuoteServiceGetQuotesProcedure:
			quoteServiceGetQuotesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedQuoteServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedQuoteServiceHandler struct{}

func (UnimplementedQuoteServiceHandler) GetQuotes(context.Context, *connect.Request[payment.GetQuotesRequest]) (*connect.Response[payment.GetQuotesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.payment.QuoteService.GetQuotes is not implemented"))
}
