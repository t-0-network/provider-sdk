# Generated from tzero/v1/payment/network.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "protobuf=google".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.compat import google_protobuf_binary_codec, google_protobuf_codecs
from connectrpc.compression.gzip import GzipCompression
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.protocol import ProtocolType
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from pyqwest import Client, SyncClient

from .network_pb2 import CompleteManualAmlCheckRequest, CompleteManualAmlCheckResponse, ConfirmPayoutRequest, ConfirmPayoutResponse, CreatePaymentRequest, CreatePaymentResponse, FinalizePayoutRequest, FinalizePayoutResponse, GetQuoteRequest, GetQuoteResponse, UpdateQuoteRequest, UpdateQuoteResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext



_DEFAULT_CODECS = google_protobuf_codecs()
_PROTO_BINARY_CODEC = google_protobuf_binary_codec()
_GZIP_COMPRESSION = GzipCompression()

class NetworkService(Protocol):
    """
    *
    Network surface for publishing quotes and initiating pay-out payments.

    All methods of this service are idempotent, meaning they are safe to retry and multiple calls with the same parameters will have no additional effect.
    """
    async def update_quote(self, request: UpdateQuoteRequest, ctx: RequestContext[UpdateQuoteRequest, UpdateQuoteResponse], /) -> UpdateQuoteResponse:
        """
        *
        Publishes pay-in and pay-out quotes (FX rates) into the network.
        Quotes carry tiered pricing bands and an expiration timestamp.
        Full snapshot: omitted quotes are deleted.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def get_quote(self, request: GetQuoteRequest, ctx: RequestContext[GetQuoteRequest, GetQuoteResponse], /) -> GetQuoteResponse:
        """
        *
        Request the best available quote for a payout in a specific currency, for a given amount.
        If the payout quote exists, but the credit limit is exceeded, this quote will not be considered.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def create_payment(self, request: CreatePaymentRequest, ctx: RequestContext[CreatePaymentRequest, CreatePaymentResponse], /) -> CreatePaymentResponse:
        """
        *
        Submit a request to create a new payment for the specified pay-out currency.
        QuoteId is the optional parameter.
        If the quoteId is specified, it must be a valid quoteId previously returned by GetQuote.
        If the quoteId is not specified, a suitable quote is selected for the pay-out currency and amount, as GetQuote does.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def confirm_payout(self, request: ConfirmPayoutRequest, ctx: RequestContext[ConfirmPayoutRequest, ConfirmPayoutResponse], /) -> ConfirmPayoutResponse:
        """
        *
        Informs the network that a payout has been completed, specifying the payment ID and payout ID
        from the original payout request.
        deprecated, use the FinalizePayout rpc instead.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def finalize_payout(self, request: FinalizePayoutRequest, ctx: RequestContext[FinalizePayoutRequest, FinalizePayoutResponse], /) -> FinalizePayoutResponse:
        """
        *
        Report the final outcome of a payout to the network, identified by the payment id from the
        original PayoutRequest, as either success (with an optional receipt) or failure with a reason.
        Supersedes the deprecated ConfirmPayout, which could only signal completion.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def complete_manual_aml_check(self, request: CompleteManualAmlCheckRequest, ctx: RequestContext[CompleteManualAmlCheckRequest, CompleteManualAmlCheckResponse], /) -> CompleteManualAmlCheckResponse:
        """
        *
        Reports the result of a manual AML check on a payment.
        On approval, the response carries the updated settlement/payout amount and quotes.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')


class NetworkServiceASGIApplication(ConnectASGIApplication[NetworkService]):
    def __init__(
        self,
        service: NetworkService | AsyncGenerator[NetworkService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = _DEFAULT_CODECS,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/tzero.v1.payment.NetworkService/UpdateQuote": Endpoint.unary(
                    method=MethodInfo(
                        name="UpdateQuote",
                        service_name="tzero.v1.payment.NetworkService",
                        input=UpdateQuoteRequest,
                        output=UpdateQuoteResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.update_quote,
                ),
                "/tzero.v1.payment.NetworkService/GetQuote": Endpoint.unary(
                    method=MethodInfo(
                        name="GetQuote",
                        service_name="tzero.v1.payment.NetworkService",
                        input=GetQuoteRequest,
                        output=GetQuoteResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=svc.get_quote,
                ),
                "/tzero.v1.payment.NetworkService/CreatePayment": Endpoint.unary(
                    method=MethodInfo(
                        name="CreatePayment",
                        service_name="tzero.v1.payment.NetworkService",
                        input=CreatePaymentRequest,
                        output=CreatePaymentResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.create_payment,
                ),
                "/tzero.v1.payment.NetworkService/ConfirmPayout": Endpoint.unary(
                    method=MethodInfo(
                        name="ConfirmPayout",
                        service_name="tzero.v1.payment.NetworkService",
                        input=ConfirmPayoutRequest,
                        output=ConfirmPayoutResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.confirm_payout,
                ),
                "/tzero.v1.payment.NetworkService/FinalizePayout": Endpoint.unary(
                    method=MethodInfo(
                        name="FinalizePayout",
                        service_name="tzero.v1.payment.NetworkService",
                        input=FinalizePayoutRequest,
                        output=FinalizePayoutResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.finalize_payout,
                ),
                "/tzero.v1.payment.NetworkService/CompleteManualAmlCheck": Endpoint.unary(
                    method=MethodInfo(
                        name="CompleteManualAmlCheck",
                        service_name="tzero.v1.payment.NetworkService",
                        input=CompleteManualAmlCheckRequest,
                        output=CompleteManualAmlCheckResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.complete_manual_aml_check,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/tzero.v1.payment.NetworkService"


class NetworkServiceClient(ConnectClient):
    """
    *
    Network surface for publishing quotes and initiating pay-out payments.

    All methods of this service are idempotent, meaning they are safe to retry and multiple calls with the same parameters will have no additional effect.
    """
    def __init__(
        self,
        address: str,
        *,
        codec: Codec | None = _PROTO_BINARY_CODEC,
        protocol: ProtocolType = ProtocolType.CONNECT,
        accept_compression: Iterable[Compression] | None = None,
        send_compression: Compression | None = _GZIP_COMPRESSION,
        timeout_ms: int | None = None,
        read_max_bytes: int | None = None,
        interceptors: Iterable[Interceptor] = (),
        http_client: Client | None = None,
    ) -> None:
        super().__init__(
            address=address,
            codec=codec,
            protocol=protocol,
            accept_compression=accept_compression,
            send_compression=send_compression,
            timeout_ms=timeout_ms,
            read_max_bytes=read_max_bytes,
            interceptors=interceptors,
            http_client=http_client,
        )
    async def update_quote(
        self,
        request: UpdateQuoteRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> UpdateQuoteResponse:
        """
        *
        Publishes pay-in and pay-out quotes (FX rates) into the network.
        Quotes carry tiered pricing bands and an expiration timestamp.
        Full snapshot: omitted quotes are deleted.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="UpdateQuote",
                service_name="tzero.v1.payment.NetworkService",
                input=UpdateQuoteRequest,
                output=UpdateQuoteResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def get_quote(
        self,
        request: GetQuoteRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> GetQuoteResponse:
        """
        *
        Request the best available quote for a payout in a specific currency, for a given amount.
        If the payout quote exists, but the credit limit is exceeded, this quote will not be considered.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="GetQuote",
                service_name="tzero.v1.payment.NetworkService",
                input=GetQuoteRequest,
                output=GetQuoteResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )

    async def create_payment(
        self,
        request: CreatePaymentRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CreatePaymentResponse:
        """
        *
        Submit a request to create a new payment for the specified pay-out currency.
        QuoteId is the optional parameter.
        If the quoteId is specified, it must be a valid quoteId previously returned by GetQuote.
        If the quoteId is not specified, a suitable quote is selected for the pay-out currency and amount, as GetQuote does.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CreatePayment",
                service_name="tzero.v1.payment.NetworkService",
                input=CreatePaymentRequest,
                output=CreatePaymentResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def confirm_payout(
        self,
        request: ConfirmPayoutRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> ConfirmPayoutResponse:
        """
        *
        Informs the network that a payout has been completed, specifying the payment ID and payout ID
        from the original payout request.
        deprecated, use the FinalizePayout rpc instead.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ConfirmPayout",
                service_name="tzero.v1.payment.NetworkService",
                input=ConfirmPayoutRequest,
                output=ConfirmPayoutResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def finalize_payout(
        self,
        request: FinalizePayoutRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> FinalizePayoutResponse:
        """
        *
        Report the final outcome of a payout to the network, identified by the payment id from the
        original PayoutRequest, as either success (with an optional receipt) or failure with a reason.
        Supersedes the deprecated ConfirmPayout, which could only signal completion.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="FinalizePayout",
                service_name="tzero.v1.payment.NetworkService",
                input=FinalizePayoutRequest,
                output=FinalizePayoutResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def complete_manual_aml_check(
        self,
        request: CompleteManualAmlCheckRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CompleteManualAmlCheckResponse:
        """
        *
        Reports the result of a manual AML check on a payment.
        On approval, the response carries the updated settlement/payout amount and quotes.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CompleteManualAmlCheck",
                service_name="tzero.v1.payment.NetworkService",
                input=CompleteManualAmlCheckRequest,
                output=CompleteManualAmlCheckResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

class NetworkServiceSync(Protocol):
    """
    *
    Network surface for publishing quotes and initiating pay-out payments.

    All methods of this service are idempotent, meaning they are safe to retry and multiple calls with the same parameters will have no additional effect.
    """
    def update_quote(self, request: UpdateQuoteRequest, ctx: RequestContext[UpdateQuoteRequest, UpdateQuoteResponse], /) -> UpdateQuoteResponse:
        """
        *
        Publishes pay-in and pay-out quotes (FX rates) into the network.
        Quotes carry tiered pricing bands and an expiration timestamp.
        Full snapshot: omitted quotes are deleted.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def get_quote(self, request: GetQuoteRequest, ctx: RequestContext[GetQuoteRequest, GetQuoteResponse], /) -> GetQuoteResponse:
        """
        *
        Request the best available quote for a payout in a specific currency, for a given amount.
        If the payout quote exists, but the credit limit is exceeded, this quote will not be considered.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def create_payment(self, request: CreatePaymentRequest, ctx: RequestContext[CreatePaymentRequest, CreatePaymentResponse], /) -> CreatePaymentResponse:
        """
        *
        Submit a request to create a new payment for the specified pay-out currency.
        QuoteId is the optional parameter.
        If the quoteId is specified, it must be a valid quoteId previously returned by GetQuote.
        If the quoteId is not specified, a suitable quote is selected for the pay-out currency and amount, as GetQuote does.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def confirm_payout(self, request: ConfirmPayoutRequest, ctx: RequestContext[ConfirmPayoutRequest, ConfirmPayoutResponse], /) -> ConfirmPayoutResponse:
        """
        *
        Informs the network that a payout has been completed, specifying the payment ID and payout ID
        from the original payout request.
        deprecated, use the FinalizePayout rpc instead.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def finalize_payout(self, request: FinalizePayoutRequest, ctx: RequestContext[FinalizePayoutRequest, FinalizePayoutResponse], /) -> FinalizePayoutResponse:
        """
        *
        Report the final outcome of a payout to the network, identified by the payment id from the
        original PayoutRequest, as either success (with an optional receipt) or failure with a reason.
        Supersedes the deprecated ConfirmPayout, which could only signal completion.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def complete_manual_aml_check(self, request: CompleteManualAmlCheckRequest, ctx: RequestContext[CompleteManualAmlCheckRequest, CompleteManualAmlCheckResponse], /) -> CompleteManualAmlCheckResponse:
        """
        *
        Reports the result of a manual AML check on a payment.
        On approval, the response carries the updated settlement/payout amount and quotes.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')


class NetworkServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: NetworkServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = _DEFAULT_CODECS,
    ) -> None:
        super().__init__(
            endpoints={
                "/tzero.v1.payment.NetworkService/UpdateQuote": EndpointSync.unary(
                    method=MethodInfo(
                        name="UpdateQuote",
                        service_name="tzero.v1.payment.NetworkService",
                        input=UpdateQuoteRequest,
                        output=UpdateQuoteResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.update_quote,
                ),
                "/tzero.v1.payment.NetworkService/GetQuote": EndpointSync.unary(
                    method=MethodInfo(
                        name="GetQuote",
                        service_name="tzero.v1.payment.NetworkService",
                        input=GetQuoteRequest,
                        output=GetQuoteResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=service.get_quote,
                ),
                "/tzero.v1.payment.NetworkService/CreatePayment": EndpointSync.unary(
                    method=MethodInfo(
                        name="CreatePayment",
                        service_name="tzero.v1.payment.NetworkService",
                        input=CreatePaymentRequest,
                        output=CreatePaymentResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.create_payment,
                ),
                "/tzero.v1.payment.NetworkService/ConfirmPayout": EndpointSync.unary(
                    method=MethodInfo(
                        name="ConfirmPayout",
                        service_name="tzero.v1.payment.NetworkService",
                        input=ConfirmPayoutRequest,
                        output=ConfirmPayoutResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.confirm_payout,
                ),
                "/tzero.v1.payment.NetworkService/FinalizePayout": EndpointSync.unary(
                    method=MethodInfo(
                        name="FinalizePayout",
                        service_name="tzero.v1.payment.NetworkService",
                        input=FinalizePayoutRequest,
                        output=FinalizePayoutResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.finalize_payout,
                ),
                "/tzero.v1.payment.NetworkService/CompleteManualAmlCheck": EndpointSync.unary(
                    method=MethodInfo(
                        name="CompleteManualAmlCheck",
                        service_name="tzero.v1.payment.NetworkService",
                        input=CompleteManualAmlCheckRequest,
                        output=CompleteManualAmlCheckResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.complete_manual_aml_check,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/tzero.v1.payment.NetworkService"


class NetworkServiceClientSync(ConnectClientSync):
    """
    *
    Network surface for publishing quotes and initiating pay-out payments.

    All methods of this service are idempotent, meaning they are safe to retry and multiple calls with the same parameters will have no additional effect.
    """
    def __init__(
        self,
        address: str,
        *,
        codec: Codec | None = _PROTO_BINARY_CODEC,
        protocol: ProtocolType = ProtocolType.CONNECT,
        accept_compression: Iterable[Compression] | None = None,
        send_compression: Compression | None = _GZIP_COMPRESSION,
        timeout_ms: int | None = None,
        read_max_bytes: int | None = None,
        interceptors: Iterable[InterceptorSync] = (),
        http_client: SyncClient | None = None,
    ) -> None:
        super().__init__(
            address=address,
            codec=codec,
            protocol=protocol,
            accept_compression=accept_compression,
            send_compression=send_compression,
            timeout_ms=timeout_ms,
            read_max_bytes=read_max_bytes,
            interceptors=interceptors,
            http_client=http_client,
        )
    def update_quote(
        self,
        request: UpdateQuoteRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> UpdateQuoteResponse:
        """
        *
        Publishes pay-in and pay-out quotes (FX rates) into the network.
        Quotes carry tiered pricing bands and an expiration timestamp.
        Full snapshot: omitted quotes are deleted.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="UpdateQuote",
                service_name="tzero.v1.payment.NetworkService",
                input=UpdateQuoteRequest,
                output=UpdateQuoteResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def get_quote(
        self,
        request: GetQuoteRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> GetQuoteResponse:
        """
        *
        Request the best available quote for a payout in a specific currency, for a given amount.
        If the payout quote exists, but the credit limit is exceeded, this quote will not be considered.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="GetQuote",
                service_name="tzero.v1.payment.NetworkService",
                input=GetQuoteRequest,
                output=GetQuoteResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )
    def create_payment(
        self,
        request: CreatePaymentRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CreatePaymentResponse:
        """
        *
        Submit a request to create a new payment for the specified pay-out currency.
        QuoteId is the optional parameter.
        If the quoteId is specified, it must be a valid quoteId previously returned by GetQuote.
        If the quoteId is not specified, a suitable quote is selected for the pay-out currency and amount, as GetQuote does.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CreatePayment",
                service_name="tzero.v1.payment.NetworkService",
                input=CreatePaymentRequest,
                output=CreatePaymentResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def confirm_payout(
        self,
        request: ConfirmPayoutRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> ConfirmPayoutResponse:
        """
        *
        Informs the network that a payout has been completed, specifying the payment ID and payout ID
        from the original payout request.
        deprecated, use the FinalizePayout rpc instead.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ConfirmPayout",
                service_name="tzero.v1.payment.NetworkService",
                input=ConfirmPayoutRequest,
                output=ConfirmPayoutResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def finalize_payout(
        self,
        request: FinalizePayoutRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> FinalizePayoutResponse:
        """
        *
        Report the final outcome of a payout to the network, identified by the payment id from the
        original PayoutRequest, as either success (with an optional receipt) or failure with a reason.
        Supersedes the deprecated ConfirmPayout, which could only signal completion.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="FinalizePayout",
                service_name="tzero.v1.payment.NetworkService",
                input=FinalizePayoutRequest,
                output=FinalizePayoutResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def complete_manual_aml_check(
        self,
        request: CompleteManualAmlCheckRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CompleteManualAmlCheckResponse:
        """
        *
        Reports the result of a manual AML check on a payment.
        On approval, the response carries the updated settlement/payout amount and quotes.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CompleteManualAmlCheck",
                service_name="tzero.v1.payment.NetworkService",
                input=CompleteManualAmlCheckRequest,
                output=CompleteManualAmlCheckResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
